const API_URL='https://bitter-recipe-889a.drivehubler10.workers.dev/';
const $=id=>document.getElementById(id);let currentVehicle=null;
function setStatus(message,type=''){ $('status').textContent=message;$('status').className=`status ${type}`; }
function descriptionText(){return $('description').value;}
async function copyDescription(){const text=descriptionText();try{await navigator.clipboard.writeText(text);return true;}catch{const ta=document.createElement('textarea');ta.value=text;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.focus();ta.select();document.execCommand('copy');ta.remove();return true;}}
function renderPhotos(photos=[]){const box=$('photos');box.innerHTML='';photos.forEach((url,i)=>{const item=document.createElement('div');item.className='photo selected';item.innerHTML=`<label><input type="checkbox" checked data-photo-index="${i}"></label><img src="${escapeAttr(url)}" alt="Vehicle photo ${i+1}" loading="lazy"><a href="${escapeAttr(url)}" target="_blank" rel="noopener">Open photo ${i+1}</a>`;box.appendChild(item);item.querySelector('input').addEventListener('change',e=>item.classList.toggle('selected',e.target.checked));});$('photoCount').textContent=`${photos.length} photos`;}
function selectedPhotos(){return currentVehicle?.photos?.filter((_,i)=>document.querySelector(`[data-photo-index="${i}"]`)?.checked)||[];}
function proxyUrl(url){return API_URL+'?action=image&url='+encodeURIComponent(url);}
async function downloadSelectedImages(){const urls=selectedPhotos();if(!urls.length){setStatus('Select at least one photo first.','error');return;}setStatus(`Preparing ${urls.length} selected photos...`);for(let i=0;i<urls.length;i++){try{const response=await fetch(proxyUrl(urls[i]));if(!response.ok)throw new Error();const blob=await response.blob();const objectUrl=URL.createObjectURL(blob);const a=document.createElement('a');a.href=objectUrl;a.download=`vehicle-photo-${String(i+1).padStart(2,'0')}.${extensionFromType(blob.type)}`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(objectUrl),1000);await new Promise(r=>setTimeout(r,300));}catch{setStatus(`Could not download photo ${i+1}.`,'error');return;}}setStatus(`${urls.length} selected photos downloaded.`,'ok');}
function extensionFromType(type){if(type.includes('png'))return'png';if(type.includes('webp'))return'webp';return'jpg';}
function escapeAttr(s){return String(s).replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
function fillVehicle(v){currentVehicle=v;$('vehicleHeading').textContent=v.title||'Vehicle';$('vinBadge').textContent=v.vin||'VIN unavailable';$('title').value=v.marketplaceTitle||v.title||'';$('price').value=v.price||'';$('mileage').value=v.mileage||'';$('location').value=v.location||'Indiana';$('stock').value=v.stock||'';$('vin').value=v.vin||'';$('description').value=v.description||'';renderPhotos(v.photos||[]);$('editor').classList.remove('hidden');$('photosCard').classList.remove('hidden');}
async function importVehicle(){const url=$('vehicleUrl').value.trim();if(!url)return setStatus('Paste a DriveHubler vehicle URL first.','error');$('importBtn').disabled=true;setStatus('Importing vehicle information...');try{const res=await fetch(API_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const data=await res.json();if(!data.ok)throw new Error(data.error||'Import failed.');fillVehicle(data.vehicle);setStatus('Vehicle imported successfully.','ok');}catch(err){setStatus(err.message||'Failed to fetch the vehicle. Check the Worker URL and deployment.','error');}finally{$('importBtn').disabled=false;}}
$('importBtn').addEventListener('click',importVehicle);$('vehicleUrl').addEventListener('keydown',e=>{if(e.key==='Enter')importVehicle();});$('copyBtn').addEventListener('click',async()=>{await copyDescription();setStatus('Description copied to clipboard.','ok');});$('openMarketplaceBtn').addEventListener('click',async()=>{await copyDescription();setStatus('Description copied. Opening Facebook Marketplace...','ok');window.open('https://www.facebook.com/marketplace/create/vehicle','_blank','noopener');});$('downloadPhotosBtn').addEventListener('click',downloadSelectedImages);
$('saveBtn').addEventListener('click',()=>{const saved=JSON.parse(localStorage.getItem('hublerMarketplaceVehicles')||'[]');const record={...currentVehicle,marketplaceTitle:$('title').value,price:$('price').value,mileage:$('mileage').value,location:$('location').value,stock:$('stock').value,vin:$('vin').value,description:$('description').value,savedAt:new Date().toISOString()};const key=record.vin||record.url;const filtered=saved.filter(x=>(x.vin||x.url)!==key);filtered.unshift(record);localStorage.setItem('hublerMarketplaceVehicles',JSON.stringify(filtered.slice(0,100)));renderSaved();setStatus('Vehicle saved in this browser.','ok');});
function renderSaved(){const list=JSON.parse(localStorage.getItem('hublerMarketplaceVehicles')||'[]');$('savedVehicles').innerHTML=list.length?list.map((v,i)=>`<div class="saved-item"><div><strong>${escapeAttr(v.marketplaceTitle||v.title||'Vehicle')}</strong><small>${escapeAttr(v.vin||'')}</small></div><button data-load="${i}">Load</button></div>`).join(''):'<p class="muted">No vehicles saved yet.</p>';list.forEach((_,i)=>document.querySelector(`[data-load="${i}"]`)?.addEventListener('click',()=>fillVehicle(list[i])));}renderSaved();
